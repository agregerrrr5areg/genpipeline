# Core ML stack — install torch from cu128 index for Blackwell (sm_120) support:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
torch==2.10.0+cu128
torchvision==0.25.0+cu128

# Bayesian optimisation (keep on CPU — see blackwell_compat.py)
botorch==0.17.0
gpytorch==1.15.1

# Numerics / geometry
numpy
scipy
scikit-image
trimesh
pyvista

# Pipeline utilities
pydantic
pyyaml
tqdm
matplotlib
seaborn

# Training monitoring
tensorboard
tensorboardX

# C++ / CUDA extension build
ninja
pybind11

# Testing
pytest
jaxtyping

# GPU telemetry
nvidia-ml-py
